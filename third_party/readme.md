# Third-party dependencies

Place pinned external implementations here, for example:

```text
third_party/
├── liboqs/
├── pqclean/
└── target-project/
```

Use Git submodules or a reproducible setup script and record exact commits.
